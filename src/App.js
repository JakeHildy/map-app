import axios from "axios";

const App = () => {
  const getCustomerAddresses = async () => {
    axios.get(``);
  };

  return (
    <div className="App">
      <h1>Map App</h1>
    </div>
  );
};

export default App;
